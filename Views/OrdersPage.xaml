<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="bezorgersapp.Views.OrdersPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Taken"
    Shell.NavBarIsVisible="False">

    <Grid BackgroundColor="#F5F7FB" RowDefinitions="Auto,*">
        <VerticalStackLayout Padding="20,18,20,12" Spacing="4">
            <Label
                FontAttributes="Bold"
                FontSize="12"
                Text="MATRIX INC. BEZORGAPP"
                TextColor="#2F7D6B" />
            <Label
                FontAttributes="Bold"
                FontSize="30"
                Text="Demo ritten"
                TextColor="#172033" />
            <Label
                FontSize="15"
                Text="Bestellingen die klaarstaan voor de bezorgapp."
                TextColor="#65758B" />
        </VerticalStackLayout>

        <RefreshView
            Grid.Row="1"
            Command="{Binding LoadOrdersCommand}"
            IsRefreshing="{Binding IsBusy}">
            <CollectionView ItemsSource="{Binding Orders}" Margin="20,0,20,18">
                <CollectionView.EmptyView>
                    <Border
                        Margin="0,20,0,0"
                        Padding="18"
                        BackgroundColor="#FFFFFF"
                        Stroke="#DFE6EF"
                        StrokeShape="RoundRectangle 8">
                        <Border.Shadow>
                            <Shadow Brush="#101E36" Opacity="0.06" Radius="14" Offset="0,8" />
                        </Border.Shadow>
                        <Label
                            FontSize="16"
                            HorizontalTextAlignment="Center"
                            Text="{Binding Message}"
                            TextColor="#65758B" />
                    </Border>
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Margin="0,0,0,14"
                            Padding="16"
                            BackgroundColor="#FFFFFF"
                            Stroke="#DFE6EF"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 8">
                            <Border.Shadow>
                                <Shadow Brush="#101E36" Opacity="0.06" Radius="14" Offset="0,8" />
                            </Border.Shadow>
                            <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="14">
                                <Grid ColumnDefinitions="42,*,Auto" ColumnSpacing="12">
                                    <Border
                                        BackgroundColor="#2F7D6B"
                                        HeightRequest="36"
                                        Stroke="#2F7D6B"
                                        StrokeShape="RoundRectangle 18"
                                        WidthRequest="36">
                                        <Label
                                            FontAttributes="Bold"
                                            HorizontalTextAlignment="Center"
                                            Text="{Binding Id}"
                                            TextColor="White"
                                            VerticalTextAlignment="Center" />
                                    </Border>

                                    <VerticalStackLayout Grid.Column="1" Spacing="2">
                                        <Label
                                            FontAttributes="Bold"
                                            FontSize="21"
                                            TextColor="#172033"
                                            LineBreakMode="TailTruncation"
                                            Text="{Binding Address}" />
                                        <Label
                                            FontSize="15"
                                            Text="{Binding FullAddress}"
                                            TextColor="#65758B" />
                                    </VerticalStackLayout>

                                    <Button
                                        Grid.Column="2"
                                        BackgroundColor="#FFFFFF"
                                        BorderColor="#172033"
                                        BorderWidth="1"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenOrderCommand}"
                                        CommandParameter="{Binding .}"
                                        CornerRadius="8"
                                        FontSize="14"
                                        Padding="12,8"
                                        Text="Details"
                                        TextColor="#172033" />
                                </Grid>

                                <Border
                                    Grid.Row="1"
                                    Padding="10,6"
                                    BackgroundColor="#E5F6EF"
                                    Stroke="#CBEBDD"
                                    StrokeShape="RoundRectangle 8">
                                    <Label
                                        FontAttributes="Bold"
                                        FontSize="13"
                                        Text="{Binding Status}"
                                        TextColor="#17664F" />
                                </Border>

                                <Grid Grid.Row="2" ColumnDefinitions="*,*" ColumnSpacing="12">
                                    <VerticalStackLayout Spacing="3">
                                        <Label FontSize="12" Text="Pakketten" TextColor="#65758B" />
                                        <Label FontAttributes="Bold" FontSize="16" Text="{Binding PackageSize}" TextColor="#172033" />
                                    </VerticalStackLayout>
                                    <VerticalStackLayout Grid.Column="1" Spacing="3">
                                        <Label FontSize="12" Text="Bus" TextColor="#65758B" />
                                        <Label
                                            FontAttributes="Bold"
                                            FontSize="16"
                                            TextColor="#172033"
                                            Text="{Binding VanLicensePlate}" />
                                        <Label
                                            FontSize="13"
                                            Text="{Binding VanLoadingZone}"
                                            TextColor="#65758B" />
                                    </VerticalStackLayout>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>
