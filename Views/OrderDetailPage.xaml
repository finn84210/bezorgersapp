<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="bezorgersapp.Views.OrderDetailPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Ritdetails"
    Shell.NavBarIsVisible="False">

    <ScrollView BackgroundColor="#F3F4F6">
        <VerticalStackLayout Padding="20,18" Spacing="14">
            <Grid ColumnDefinitions="80,*,80" HeightRequest="48">
                <Button
                    BackgroundColor="#FFFFFF"
                    BorderColor="#111827"
                    BorderWidth="1"
                    Clicked="BackTapped"
                    CornerRadius="8"
                    FontSize="14"
                    Text="Terug"
                    TextColor="#111827" />
                <Label
                    Grid.Column="1"
                    FontAttributes="Bold"
                    FontSize="28"
                    HorizontalTextAlignment="Center"
                    Text="Ritdetails"
                    VerticalTextAlignment="Center" />
            </Grid>

            <Border
                Padding="16"
                BackgroundColor="#FFFFFF"
                Stroke="#111827"
                StrokeShape="RoundRectangle 8">
                <VerticalStackLayout Spacing="12">
                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout Spacing="2">
                            <Label FontSize="13" Text="Afleveradres" TextColor="#6B7280" />
                            <Label
                                FontAttributes="Bold"
                                FontSize="25"
                                Text="{Binding Order.Address}" />
                            <Label
                                FontSize="16"
                                Text="{Binding Order.FullAddress}"
                                TextColor="#4B5563" />
                        </VerticalStackLayout>
                        <Border
                            Grid.Column="1"
                            Padding="10,6"
                            BackgroundColor="#F9FAFB"
                            Stroke="#D1D5DB"
                            StrokeShape="RoundRectangle 8">
                            <Label
                                FontAttributes="Bold"
                                FontSize="12"
                                Text="{Binding Order.Status}" />
                        </Border>
                    </Grid>

                    <BoxView HeightRequest="1" Color="#E5E7EB" />

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                        <VerticalStackLayout Spacing="2">
                            <Label FontSize="12" Text="Klant" TextColor="#6B7280" />
                            <Label FontAttributes="Bold" FontSize="16" Text="{Binding Order.CustomerName}" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="1" Spacing="2">
                            <Label FontSize="12" Text="Referentie" TextColor="#6B7280" />
                            <Label FontAttributes="Bold" FontSize="16" Text="{Binding Order.ExternalReference}" />
                        </VerticalStackLayout>
                    </Grid>

                    <Border Padding="12" BackgroundColor="#F9FAFB" Stroke="#D1D5DB" StrokeShape="RoundRectangle 8">
                        <VerticalStackLayout Spacing="2">
                            <Label FontSize="12" Text="Opmerking" TextColor="#6B7280" />
                            <Label FontSize="15" Text="{Binding Order.Notes}" />
                        </VerticalStackLayout>
                    </Border>

                    <Button
                        BackgroundColor="#111827"
                        Command="{Binding OpenMapsCommand}"
                        CornerRadius="8"
                        FontAttributes="Bold"
                        Padding="14"
                        Text="Route openen in Google Maps"
                        TextColor="White" />
                </VerticalStackLayout>
            </Border>

            <Border Padding="16" BackgroundColor="#FFFFFF" Stroke="#111827" StrokeShape="RoundRectangle 8">
                <VerticalStackLayout Spacing="12">
                    <Label FontAttributes="Bold" FontSize="20" Text="Businformatie" />
                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="12" RowSpacing="12">
                        <VerticalStackLayout Spacing="2">
                            <Label FontSize="12" Text="Bus" TextColor="#6B7280" />
                            <Label FontAttributes="Bold" FontSize="16" Text="{Binding Order.VanName}" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="1" Spacing="2">
                            <Label FontSize="12" Text="Kenteken" TextColor="#6B7280" />
                            <Label FontAttributes="Bold" FontSize="16" Text="{Binding Order.VanLicensePlate}" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Row="1" Spacing="2">
                            <Label FontSize="12" Text="Laadplek" TextColor="#6B7280" />
                            <Label FontAttributes="Bold" FontSize="16" Text="{Binding Order.VanLoadingZone}" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Row="1" Grid.Column="1" Spacing="2">
                            <Label FontSize="12" Text="Brandstof" TextColor="#6B7280" />
                            <Label FontAttributes="Bold" FontSize="16" Text="{Binding Order.VanFuelLevel}" />
                        </VerticalStackLayout>
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <Border Padding="16" BackgroundColor="#FFFFFF" Stroke="#111827" StrokeShape="RoundRectangle 8">
                <VerticalStackLayout Spacing="12">
                    <Grid ColumnDefinitions="*,Auto">
                        <Label FontAttributes="Bold" FontSize="20" Text="Pakketten controleren" />
                        <Border
                            Grid.Column="1"
                            Padding="10,5"
                            BackgroundColor="#F9FAFB"
                            Stroke="#D1D5DB"
                            StrokeShape="RoundRectangle 8">
                            <Label FontSize="12" Text="{Binding PackageCheckText}" />
                        </Border>
                    </Grid>

                    <CollectionView ItemsSource="{Binding Order.Packages}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Border
                                    Margin="0,0,0,8"
                                    Padding="10"
                                    BackgroundColor="#F9FAFB"
                                    Stroke="#D1D5DB"
                                    StrokeShape="RoundRectangle 8">
                                    <Grid ColumnDefinitions="42,*">
                                        <CheckBox
                                            IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                            CheckedChanged="PackageCheckedChanged" />
                                        <Label
                                            Grid.Column="1"
                                            FontSize="15"
                                            Text="{Binding Description}"
                                            VerticalTextAlignment="Center" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>

            <Border Padding="16" BackgroundColor="#FFFFFF" Stroke="#111827" StrokeShape="RoundRectangle 8">
                <VerticalStackLayout Spacing="12">
                    <Label FontAttributes="Bold" FontSize="20" Text="Status en bewijs" />
                    <Border Padding="12,4" BackgroundColor="#F9FAFB" Stroke="#D1D5DB" StrokeShape="RoundRectangle 8">
                        <Picker
                            FontSize="16"
                            ItemsSource="{Binding StatusOptions}"
                            SelectedItem="{Binding SelectedStatus}" />
                    </Border>

                    <Button
                        BackgroundColor="#111827"
                        Command="{Binding UpdateStatusCommand}"
                        CornerRadius="8"
                        FontAttributes="Bold"
                        Padding="14"
                        Text="Status opslaan"
                        TextColor="White" />

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Button
                            BackgroundColor="#FFFFFF"
                            BorderColor="#111827"
                            BorderWidth="1"
                            Command="{Binding GetLocationCommand}"
                            CornerRadius="8"
                            Padding="12"
                            Text="GPS ophalen"
                            TextColor="#111827" />
                        <Button
                            Grid.Column="1"
                            BackgroundColor="#FFFFFF"
                            BorderColor="#111827"
                            BorderWidth="1"
                            Command="{Binding TakePhotoCommand}"
                            CornerRadius="8"
                            Padding="12"
                            Text="Foto maken"
                            TextColor="#111827" />
                    </Grid>

                    <Border Padding="12" BackgroundColor="#F9FAFB" Stroke="#D1D5DB" StrokeShape="RoundRectangle 8">
                        <Label FontSize="14" Text="{Binding LocationText}" TextColor="#4B5563" />
                    </Border>

                    <Image
                        Aspect="AspectFill"
                        BackgroundColor="#E5E7EB"
                        HeightRequest="170"
                        Source="{Binding DeliveryPhoto}" />
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
